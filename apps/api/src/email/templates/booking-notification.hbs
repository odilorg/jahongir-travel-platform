<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Booking</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #3b82f6;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9fafb;
      padding: 30px;
      border-radius: 0 0 5px 5px;
    }
    .alert {
      background-color: #fef3c7;
      border-left: 4px solid #f59e0b;
      padding: 15px;
      margin-bottom: 20px;
      border-radius: 5px;
    }
    .info-row {
      margin-bottom: 15px;
      padding: 10px;
      background-color: white;
      border-radius: 5px;
    }
    .label {
      font-weight: bold;
      color: #374151;
      display: block;
      margin-bottom: 5px;
    }
    .value {
      color: #6b7280;
    }
    .total {
      background-color: #10b981;
      color: white;
      padding: 15px;
      text-align: center;
      border-radius: 5px;
      margin: 20px 0;
      font-size: 20px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🎉 New Booking Received!</h1>
  </div>

  <div class="content">
    <div class="alert">
      <strong>⚡ Action Required:</strong> A new booking has been submitted and requires confirmation.
    </div>

    <h3>Booking Details:</h3>

    <div class="info-row">
      <span class="label">Booking ID:</span>
      <span class="value">#{{bookingId}}</span>
    </div>

    <div class="info-row">
      <span class="label">Tour:</span>
      <span class="value">{{tourTitle}}</span>
    </div>

    <div class="info-row">
      <span class="label">Date:</span>
      <span class="value">{{date}}</span>
    </div>

    <div class="info-row">
      <span class="label">Number of People:</span>
      <span class="value">{{numberOfPeople}}</span>
    </div>

    <div class="total">
      Total Amount: ${{totalPrice}}
    </div>

    <h3>Customer Information:</h3>

    <div class="info-row">
      <span class="label">Name:</span>
      <span class="value">{{customerName}}</span>
    </div>

    <div class="info-row">
      <span class="label">Email:</span>
      <span class="value">{{customerEmail}}</span>
    </div>

    <p style="margin-top: 30px;">
      <strong>📞 Next Steps:</strong><br>
      1. Review the booking details<br>
      2. Contact the customer for confirmation<br>
      3. Update the booking status in the admin panel
    </p>
  </div>
</body>
</html>
